<UserControl x:Class="Manage_Competitions.Views.MainApplicationViews.Menu.HeaderMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Manage_Competitions.Views.MainApplicationViews.Menu"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid>
        <Grid VerticalAlignment="Top" Height="100" Background="#03a9f4" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Background="Transparent" Margin="10 0 10 0">
                    <materialDesign:PackIcon Kind="CollapseAll"/>
                </Button>
                <Button Background="Transparent" Margin="10 0 10 0">
                    <materialDesign:PackIcon Kind="Fullscreen"/>
                </Button>
                <Button Background="Transparent" Margin="10 0 10 0">
                    <materialDesign:PackIcon Kind="Power"/>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
